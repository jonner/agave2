bin_PROGRAMS=agave

HEADER_FILES = color.h scheme.h i-scheme-generator.h scheme-generators.h
SOURCE_FILES = agave.cc color.cc scheme.cc scheme-generators.cc
agave_SOURCES = $(HEADER_FILES) $(SOURCE_FILES)

AM_CXXFLAGS=$(AGAVE_CFLAGS)
agave_LDADD=$(AGAVE_LIBS)
